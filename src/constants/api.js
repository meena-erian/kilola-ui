const url = 'https://kilola-beta.portacode.com/api/v1';
const session_cookie_name = 'kilola-session'

const api = {
    login: '/login',
    user: '/user'
}

export {
    url,
    api,
    session_cookie_name
};